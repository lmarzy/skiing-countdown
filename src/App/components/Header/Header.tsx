import * as React from 'react';
import './styles.scss';

const Header = () => (
  <header className="site-header">
    <div className="l-container">
      <h1 className="site-header__title">Skiing/Breakage - Countdown</h1>
    </div>
  </header>
);

export default Header;
